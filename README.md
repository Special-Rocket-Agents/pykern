# PyKern
Python script emulating a command-line kernel with an online package manager

# Setup
For now, download, unzip, meet requirements.txt and run main.py NOT SETUP.PY. RUN MAIN.PY

ensure internet connection and github is NOT banned and that you CAN access github API
